<!DOCTYPE html>
<html lang="en" dir="ltr">

<head>
  <meta charset="utf-8">
  <title></title>
  <link rel="stylesheet" href="https://stackpath.bootstrapcdn.com/bootstrap/4.3.1/css/bootstrap.min.css" integrity="sha384-ggOyR0iXCbMQv3Xipma34MD+dH/1fQ784/j6cY/iJTQUOhcWr7x9JvoRxT2MZw1T" crossorigin="anonymous"/>
  <link rel="stylesheet" href="/styels.css"/>
</head>

<% include ../partials/head.ejs %>
<div class="container">


<div class="media">
  <% if(blog.img!=""){ %>
      <img src="<%=blog.img%>" class="align-self-center mr-3" alt="Blog img" style="width:20vw; height:20vh">
  <% } %>

  <div class="media-body">
    <h5 class="mt-0 text-center"><%=blog.title%></h5>
    <p class="mb-0"> <%=blog.body%> </p>
    <p class="mb-0 text-center"> Created By: <%=blog.createdBy%> </p>
<!-- Date conversion to readable format -->

<p class="mb-0 text-center"> Created On:
<%= blog.createdAt.toDateString() %>
 </p>
 <p class="mb-0 text-center"> Last Updated On:
 <%= blog.updatedAt.toDateString() %>
  </p>

  <form  class="text-center" action="/blogs/<%=blog.id%>?_method=PATCH" method="POST">
     <button class='button button-primary align-center' type="submit"><%=blog.likes%> Likes </button>
   </form>
   </div>
</div>




     <form  action="/blogs/comments/<%=blog.id%>" method="POST">
       <div class="form-group">
   <label for="exampleFormControlTextarea1">Comments</label>
   <textarea class="form-control" id="exampleFormControlTextarea1" name="comments" rows="3"></textarea>
   <button class='button button-primary align-center' type="submit">Submit Comment</button>
 </div>
      </form>
<ul class="list-group list-group-flush">
<% for(let x=blog.comments.length-1; x >= 0; x--){ %>
  <li class="list-group-item"><%= blog.comments[x] %></li>
<%}%>
</ul>
  <!-- </fieldset> -->

  <nav>
    <a href="/blogs">Back to Blogs</a>
  </nav>
  </div>
</body>

</html>
